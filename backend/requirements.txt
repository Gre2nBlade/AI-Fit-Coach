# --- REST API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- HTTP client (OpenRouter) ---
httpx==0.28.1

# --- ORM + async SQLite driver ---
SQLAlchemy==2.0.36
aiosqlite==0.20.0

# --- Migrations (подключается позже) ---
alembic==1.14.0

# --- Config / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0   # для pydantic EmailStr

# --- Scheduler ---
APScheduler==3.11.0

# --- Auth: JWT + хеширование паролей ---
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# Драйвер PostgreSQL — раскомментировать при переезде с SQLite
# asyncpg==0.30.0
